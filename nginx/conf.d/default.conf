upstream frontend {
    server frontend:80;
}

upstream backend {
    server backend:9000;
}

# HTTP Configuration (avant SSL)
server {
    listen 80;
    server_name tchim.fr www.tchim.fr;

    # Pour Certbot (validation Let's Encrypt)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirection vers HTTPS (décommenter après avoir obtenu les certificats)
    # location / {
    #     return 301 https://$host$request_uri;
    # }


    location ~ ^/api(/|$) {

        rewrite ^/api(/.*)?$ $1 break;
        
        fastcgi_pass backend;
        fastcgi_split_path_info ^(.+\.php)(/.*)$;
        include fastcgi_params;
        
        fastcgi_param SCRIPT_FILENAME /var/www/html/public/index.php;
        fastcgi_param DOCUMENT_ROOT /var/www/html/public;
        fastcgi_param PATH_INFO $fastcgi_path_info;
        fastcgi_param HTTPS off;
        
        fastcgi_pass_header Set-Cookie;
        fastcgi_pass_header Cookie;
        
        fastcgi_buffer_size 128k;
        fastcgi_buffers 4 256k;
        fastcgi_busy_buffers_size 256k;
    }

    location / {
        proxy_pass http://frontend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# HTTPS Configuration (à décommenter après obtention des certificats SSL)
# server {
#     listen 443 ssl http2;
#     server_name tchim.fr www.tchim.fr;
#
#     # Certificats SSL
#     ssl_certificate /etc/letsencrypt/live/tchim.fr/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/tchim.fr/privkey.pem;
#     
#     # Configuration SSL moderne
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384';
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     # API Backend Symfony
#     location ~ ^/api(/|$) {
#         rewrite ^/api(/.*)?$ $1 break;
#         
#         fastcgi_pass backend;
#         fastcgi_split_path_info ^(.+\.php)(/.*)$;
#         include fastcgi_params;
#         
#         fastcgi_param SCRIPT_FILENAME /var/www/html/public/index.php;
#         fastcgi_param DOCUMENT_ROOT /var/www/html/public;
#         fastcgi_param PATH_INFO $fastcgi_path_info;
#         fastcgi_param HTTPS on;
#         
#         # Support des cookies JWT
#         fastcgi_pass_header Set-Cookie;
#         fastcgi_pass_header Cookie;
#         
#         fastcgi_buffer_size 128k;
#         fastcgi_buffers 4 256k;
#         fastcgi_busy_buffers_size 256k;
#     }
#
#     # Frontend Vue.js
#     location / {
#         proxy_pass http://frontend;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
#
#     # Security headers
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#     add_header Referrer-Policy "no-referrer-when-downgrade" always;
# }