
# PostgreSQL
POSTGRES_DB=homee_prod
POSTGRES_USER=homee_user
POSTGRES_PASSWORD=CHANGEZ_MOI_AVEC_UN_MOT_DE_PASSE_FORT

# Symfony
APP_ENV=prod
APP_SECRET=GENERER_AVEC_openssl_rand_hex_32
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@database:5432/${POSTGRES_DB}?serverVersion=16&charset=utf8

# JWT Authentication
JWT_SECRET_KEY=/var/www/html/config/jwt/private.pem
JWT_PUBLIC_KEY=/var/www/html/config/jwt/public.pem
JWT_PASSPHRASE=VOTRE_PASSPHRASE_JWT

# CORS (votre domaine)
CORS_ALLOW_ORIGIN=^https?://(tchim\.fr|www\.tchim\.fr)(:[0-9]+)?$

# Frontend API URL
VITE_API_URL=https://tchim.fr/api

# Domain & SSL
DOMAIN=tchim.fr
LETSENCRYPT_EMAIL=latchimzer@gmail.com